action (findCocktail) {
  description (Searching Cocktail Information)
  type(Search)

  collect{
    input (id){
      type (Id) //변수명
      min (Optional) max (One)
    }

    input(subText){
      type(SubText)
      min (Optional) max(One)
    }
  }
  
  output (Cocktail)
}
