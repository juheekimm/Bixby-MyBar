result-view{
	match: Recipe (recipe) {
		from-output: findRecipe
	}
	message{
		template ("레시피 페이지야")
	}
	render{
		layout-match (recipe) {
			mode (Details)
		}
	}
	conversation-drivers{
		conversation-driver {
			template ("[#{value(recipe.cocktail.id)}] 정보 알려줘")
		}
	}
}