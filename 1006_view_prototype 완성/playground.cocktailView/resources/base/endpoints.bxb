endpoints {
  action-endpoints {
    action-endpoint (findCocktail) {
      accepted-inputs (name)
      local-endpoint (findCocktail.js)
    }
    action-endpoint (findRecipe){
      accepted-inputs ()
      local-endpoint (findRecipe.js)
    }
    
  }
}
